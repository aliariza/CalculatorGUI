# CMake build folders
build*/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
Makefile
*.cmake
compile_commands.json
install_manifest.txt

# macOS
.DS_Store

# Qt / moc / rcc / uic outputs (often under build, but safe)
moc_*.cpp
qrc_*.cpp
ui_*.h

# dist / packaging
dist/
dmgroot/
*.dmg
*.zip
